<%= form_for article, url: url do |f| %>
  <% if article.errors.any? %>
    <div id='error_explanation'>
      <h2><%= pluralize(article.errors.count, 'error') %> prohibited this rticle from being saved:</h2>

      <ul>
      <% article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class='actions'>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class='actions'>
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

  <div class='actions'>
    <%= f.submit %>
  </div>
<% end %>
